{
  "name": "@zoralabs/zora-rewards",
  "version": "0.1.11",
  "repository": "https://github.com/ourzora/zora-rewards.git",
  "license": "MIT",
  "files": [
    "dist/"
  ],
  "scripts": {
    "prepack": "mkdir -p dist && cp -r src/ dist/contracts && cp -r addresses/ dist/addresses && cp -r out/ dist/artifacts/"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20.3.2",
    "@wagmi/cli": "^1.3.0"
  }
}
